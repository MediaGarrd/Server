package wellatleastitried.mediagarrd.services.config;

// For services that require only one path to backup, this can be used.
// Otherwise, create a new config for a service-specific config.
public class CommonServiceConfig extends AbstractServiceConfig {}
